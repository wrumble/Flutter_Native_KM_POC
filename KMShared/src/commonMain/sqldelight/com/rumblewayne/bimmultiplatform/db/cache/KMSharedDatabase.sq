CREATE TABLE Color (
  id TEXT NOT NULL,
  name TEXT NOT NULL,
  hex TEXT NOT NULL
);

saveColor:
INSERT OR REPLACE INTO Color (id, name, hex)
VALUES (?, ?, ?);

getColorWithId:
SELECT * FROM Color WHERE id = ?;

deleteColorWithId:
DELETE FROM Color WHERE id = ?;